name: Runner Smoke Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: Identify runner
        run: |
          whoami
          hostname
          uname -a

      - name: Workspace info
        run: |
          pwd
          df -h
